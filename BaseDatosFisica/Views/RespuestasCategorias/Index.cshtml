@model IEnumerable<BaseDatosFisica.Models.RespuestaCategoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.CategoriaNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Respuesta.Texto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria.CategoriaNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Respuesta.Texto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RespuestaCategoriaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RespuestaCategoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RespuestaCategoriaID })
        </td>
    </tr>
}

</table>
